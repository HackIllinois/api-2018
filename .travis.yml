language: node_js
node_js:
  - "4.6"
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
cache:
  directories:
    - node_modules
services:
  #- docker
before_script:
  #Docker setup goes here
  - cp config/test.config.template config/test/test.config 
  - npm install
script: test
